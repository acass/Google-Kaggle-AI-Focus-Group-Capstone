{
  "doc_path": "docs/ARCHITECTURE.md",
  "claims_checked": 43,
  "claims_passed": 42,
  "claims_failed": 1,
  "failures": [
    {
      "line": 113,
      "claim": "SessionNotifier (Riverpod `Notifier`) ... folds each `StreamEvent` into `FocusGroupSession`",
      "expected": "SessionNotifier extends Notifier<FocusGroupSession>",
      "actual": "SessionNotifier extends Notifier<FocusGroupState> — FocusGroupState is the Notifier's state type (a wrapper holding a FocusGroupSession? field); events are folded into FocusGroupState, not directly into FocusGroupSession"
    }
  ]
}
